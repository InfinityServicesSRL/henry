<?xml version="1.0" encoding="utf-8"?>
<odoo>
    <data noupdate="1">

        <!-- Etapa 0: relanzable desde la interfaz sin esperar un rebuild.
             Ajustes > Tecnico > Acciones de servidor -->
        <record id="action_server_etapa0_baselines" model="ir.actions.server">
            <field name="name">AG Intelligence: congelar baselines y recomputar mediciones</field>
            <field name="model_id" ref="model_ags_baseline"/>
            <field name="state">code</field>
            <field name="code">
resumen = env["ags.baseline"].accion_congelar_y_recomputar()
log("Etapa 0: %s" % resumen)
            </field>
        </record>

    </data>
</odoo>
